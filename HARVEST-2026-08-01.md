# HARVEST 2026-08-01 — propuestas consolidadas (pendientes de aprobación)

Fuente: análisis completo de `projects/*/` (canal A: layer append/lessons/review-checks
+ retros; canal B: overrides). 7 proyectos con material: clinica-dental-2026,
guai-home-marketplace, kuraka-control, facturacion-honorarios, sie-v2, adela,
dbcanvas, sie-integraciones. ~95 candidatos brutos → 63 consolidados tras dedupe.
~60 ítems adicionales verificados como YA INTEGRADOS en el vault (con archivo:línea);
~70 clasificados como tuning de proyecto (se quedan en su layer).

Estado por ítem: `PROPUESTO` hasta aprobación del usuario. Nada se aplica sin ella.

> **2026-08-01 — TIER 1 (P1–P15) APROBADO por el usuario e INTEGRADO** (detalle
> de archivos tocados en `SUITE-CHANGELOG.md` v1.1.0). Tier 2 (P16–P83) y Tier 3
> (P84) siguen PROPUESTOS — pendientes de aprobación, recomendado aplicarlos por
> grupos de agente en próximas sesiones.

---

## TIER 1 — Integrar ya (incidente real, ≥2 proyectos, o propuesta framework re-propuesta ≥3 retros sin aterrizar)

### P1 — Reescribir `rules/16-agent-backup.md` (fix de seguridad de datos)
- **Evidencia:** kuraka-control, incidente 2026-06-13 (un subagente siguió la regla montada y ejecutó `rsync -a --delete` que borró el mirror de docs de otro proyecto). La regla actual además contradice la Fase 7 de `skills/kuraka.md`.
- **Cambio:** backup solo-orquestador vía `kuraka-backup.py`; subagentes NUNCA sincronizan el vault; nunca `--delete` a rutas compartidas; targets namespaced por proyecto; eliminar rutas sie_v2 hardcodeadas.

### P2 — Git de solo lectura para agentes de revisión/verificación
- **Evidencia:** guai (incidente REQ-20260721: `git merge` no intencionado a mitad de review dejó la rama en conflicto). El bloque existe idéntico en 6 append.md de guai; cero menciones en el vault.
- **Cambio:** una regla compartida en `skills/kuraka-policies.md` + línea por agente revisor: solo `status/log/diff/show/branch --list/rev-parse`; nunca `merge/pull/checkout/reset/rebase/cherry-pick/stash pop/commit/push`; árbol sucio al entrar = BLOCKER a reportar, jamás a resolver.

### P3 — Gate de aterrizaje de patches en el cierre de ciclo (Fase 7)
- **Evidencia:** guai (9→5 patches propuestos, casi todos sin aterrizar, 3 ciclos seguidos) + facturacion-honorarios (0/11 aterrizados; tier framework 0% histórico, mismos fixes re-propuestos 4 retros).
- **Cambio:** `final-auditor`/`run-audit` §0 pasa de "flag y aplica si es seguro" a GATE: patch de project-layer se APLICA en el cierre y se verifica con el comando de evidencia que el retro declara (`grep -c`, `test -f`), o lleva un rechazo del usuario por escrito; ≥2 retros sin aplicar = obligatorio; tras aplicar, sync al vault. Patches de framework se registran como OPEN FRAMEWORK DEBT con contador de ocurrencias (los cierra `/kuraka-harvest`). Un LL sin su fila en INDEX.md es invisible: la fila viaja en el mismo cambio.

### P4 — Protocolo de confianza de digests (T1/T9)
- **Evidencia:** guai (peor fuga de presupuesto recurrente, ≥6 ciclos: story-refiner re-verifica cada claim del digest — 287K/77 tools en un run) + sie-integraciones (digest de otro codebase contaminó un brief: ~406K desperdiciados).
- **Cambio:** (a) el orquestador estampa `VERIFIED` en un digest cuando reprodujo los claims él mismo (T9); digest estampado se consume sin re-leer los archivos citados; sin estampar = pista, no verdad. (b) Techo duro de contexto por agente ("si el paquete lo excede antes del primer tool call, pide al orquestador que lo acote"). (c) En re-scope (otro codebase/módulo/rama), el digest se RECONSTRUYE desde los archivos reales del objetivo y cada ancla se re-verifica.

### P5 — Overhaul de la regla T10 (deltas)
- **Evidencia:** facturacion-honorarios, 3-4 retros: 117K re-formulando UNA línea vía resume (vs 52K para 5 fixes con agente nuevo); un "delta" de 281K vs objetivo 30-50K.
- **Cambio en `rules/17` T10:** (a) resume re-factura todo el transcript → deltas pequeños van a agente NUEVO con prompt quirúrgico; resume solo si el razonamiento necesario vive únicamente en el transcript; (b) clasificar el patch antes de presupuestar: solo lo textual y acotado es barato; generar código/config normativo nuevo = presupuesto de run completo; lotes mixtos se parten; (c) tiers T10-S (1 archivo ≤3 edits: 30-50K) / T10-M (≤3 archivos ≤10 findings: 90-130K) / T10-L (más → re-run de fase, T10 no aplica).

### P6 — Staleness de imagen/entorno de test (rebuild tras deps/migraciones)
- **Evidencia:** adela (2 falsos verdes en un ciclo: "89 passed" con dep faltante; node_modules del host con binarios de otra plataforma) + guai (~179 fallos fantasma por capa `COPY . .` stale; propuesto 3+ retros).
- **Cambio:** `rules/17` T9 + `kuraka-policies` §green + `backend-developer` + `deployment-verifier`: story que toca manifest/lockfile/migraciones ⇒ rebuild (no-cache si migraciones) antes de confiar en el gate; la reproducción del gate corre en entorno limpio no sombreado por el host; ráfaga de `Can't locate revision`/`column does not exist` = regresión FALSA de imagen stale (rebuild, no perseguir); el verifier confirma deps nuevas presentes en la imagen.

### P7 — Probe de entorno por gate con re-poll (0 tokens)
- **Evidencia:** sie-v2, 3 ciclos (daemon caído reportado como arriba → turnos perdidos; colisión de puerto 5434 cazada a costo 0).
- **Cambio:** `skills/kuraka.md`: antes de CADA gate que ejecuta tests (3.9, por-story 4a, Fase 6, retro-gates), probe determinista: daemon de contenedores arriba + puerto de test-DB libre; en fallo, imprimir remediación exacta y RE-POLL hasta verde — nunca avanzar con un "ya está corriendo" verbal.

### P8 — Fixes de `aggregate-telemetry.py`
- **Evidencia:** facturacion-honorarios, propuesto en 4 retros consecutivos (dashboard congelado desde 2026-04 durante 9 ciclos / 6.94M tokens en volumen NTFS/exFAT) + kuraka-control S12 (promedios 0.0s falsos).
- **Cambio (código, 3 líneas):** filtrar `._*` en los globs; añadir `UnicodeDecodeError` a los excepts; `duration_ms` null → `n/a` y fuera de promedios.

### P9 — Scratch dirs de herramientas en el .gitignore del mount
- **Evidencia:** guai, propuesto 3 retros (SYS-5), confirmado ausente de `kuraka-mount.py`.
- **Cambio:** añadir `.playwright-mcp/`, `.pytest_cache/` (y equivalentes) a la plantilla de gitignore que escribe el mount.

### P10 — Invalidación de caché por namespace (check dirigido)
- **Evidencia:** 3 proyectos (adela LL-002, copia en dbcanvas, sie-v2 LL-002 + review-check §1) — bug class que los unit tests estructuralmente no cazan.
- **Cambio:** `code-reviewer` check dirigido: por cada `cache.invalidate(KEY)` del diff, grep del namespace (`KEY:*`); TODAS las subkeys hermanas se invalidan juntas (o invalidación por patrón); subkey nueva ⇒ actualizar los sitios de invalidación existentes; flag a tests ciegos de completitud (`assert_called_once`).

### P11 — Tabla de paridad al replicar flujos / añadir un segundo camino
- **Evidencia:** facturacion-honorarios (la clase recurrió 3×: RW1, B1, #5/#6 — un LL en prosa no bastó) + guai S14 (re-implementación "como el flujo X" requirió rework post-smoke).
- **Cambio:** `story-refiner` + `architect-reviewer`: historia que replica un flujo o añade un camino paralelo (segundo auth path, provider, población) ⇒ tabla campo-por-campo: cada write del camino original (`file:line` + expresión) vs el nuevo, incluidos campos fallback-only y de auditoría; fila sin paridad se justifica por escrito o es BLOCKER. En el lado historia: enumerar affordances de la pantalla de referencia y marcar cada una INCLUDE/EXPLICITLY-EXCLUDE (prefill, validaciones, pickers, estados, sub-flujos implícitos del cambio de modo).

### P12 — Smoke ejecutado por el usuario = gate bloqueante (PENDING-SMOKE)
- **Evidencia:** facturacion-honorarios ENTRAID-FIX (el defecto más caro del corpus: "agente no puede ejecutarlo" se leyó como "no se puede ejecutar" → 5 defectos apilados sobrevivieron un ciclo "cerrado"; costó un ciclo extra + 5 iteraciones con el usuario).
- **Cambio:** `skills/kuraka.md` §6.8: si el smoke no es automatizable desde el host del agente pero el usuario puede correr la app, NO se omite: el agente escribe el escenario (precondiciones, pasos, aserciones) ANTES, el usuario ejecuta y reporta, el agente registra observado + veredicto. Ciclos auth/sesión/seguridad: gate bloqueante sin DEFERRED — se cierra `PENDING-SMOKE`, nunca DONE. Los handoffs citan el veredicto textual, jamás una paráfrasis numérica.

### P13 — Role-lock de subagentes
- **Evidencia:** guai REQ-20260722 (una invocación de Fase 2.5 actuó de orquestador y no produjo nada: 66K perdidos; la línea explícita eliminó la confusión después).
- **Cambio:** preámbulo compartido (`agents/contexts/` o plantilla de invocación en `kuraka-policies`): "Eres un subagente especialista dentro de un ciclo Kuraka en curso — la fase + inputs de tu prompt SON tu tarea. No leas `kuraka.md`/`kuraka-modes.md`, no propongas pipeline, no preguntes cuál es el requerimiento; si falta un artefacto de entrada, haz UNA pregunta precisa."

### P14 — Reglas de evidencia (R-CUERPO / R-CONTROL / R-ESPEJO)
- **Evidencia:** facturacion-honorarios USREMAIL: 6 instancias en un ciclo entre 3 agentes + orquestador (REQ sobre premisa falsa "no existe validador"; finding falso desde un comentario sobre cuerpo no-op; freeze cuyo header rechazaba F-5 mientras el cuerpo lo implementaba).
- **Cambio:** nueva `rules/19-evidence.md` (+ referencias en po-analyst, code-reviewer, final-auditor): (a) claims de comportamiento citan el CUERPO ejecutable `file:start-end` — nombre, comentario, tabla de decisión o reporte de otro agente no son evidencia; (b) todo claim de no-existencia lleva grep sobre la OPERACIÓN + CONTROL POSITIVO pegado junto al "0 matches"; (c) al editar un artefacto congelado por decisión de gate: grep-y-fix de cada ocurrencia en el cuerpo PRIMERO, tabla/resumen AL FINAL.

### P15 — No-paráfrasis de contratos congelados + validación por parser real
- **Evidencia:** facturacion-honorarios (2 defectos por divergencia de prompt; un comentario XML con `--` rompió todo el Web.config — 8 errores en cascada con el usuario en un live-debug; 5 gates de grep rotos en un ciclo).
- **Cambio:** `skills/kuraka.md` + `rules/17`: prompts de impl/fix referencian SCHEMA-FROZEN por ancla y pegan el bloque literal + delta — nunca firmas/wiring reescritos; todo texto que leerá un parser (XML/JSON/YAML/SQL/config) se pega en sintaxis final o se delega; tras editar config, validar con el parser real a 0 tokens (nunca "leyendo"); presencia/ausencia en configs se afirma con parser, no grep.

---

## TIER 2 — Integrar (generalizable, evidencia fuerte de 1 proyecto)

### po-analyst / Fase 1
- **P16** Censo de datos vivos en GATE0 para fuentes externas de datos (leer N registros reales antes del REQ). [kuraka-control LL-007/010; evitó doble GATE0 ~40-80K]
- **P17** Bootstrap del primer principal privilegiado en ciclos auth (seed idempotente en scope). [adela LL-010; producto no arrancaba]
- **P18** Reproducción-first para incidentes y defectos de corrupción silenciosa: localizar el caso real + señal decisiva en logs/oráculo externo ANTES de cerrar scope. [sie-integraciones DD1243 + sie-v2 DD-1397; colapsó 2 mecanismos a 1]
- **P19** Enumeración exhaustiva de directorios en REQs de integración/módulo (`find`, clasificar cada archivo in/out/boundary). [sie-v2; archivo de 765 LOC omitido → 4 correcciones humanas]
- **P20** Spec de valores generados async por recurso separado (qué recurso lo tiene, clave de ruteo, timeout, ausente del webhook; 202 ≠ resultado vacío). [guai FIX-18/19/20]
- **P21** Completitud de ciclos de borrado: grep prueba uso SOLO por el path borrado; KEEP-list; tests re-escritos a vivo, no borrados; sweep post-borrado + boot smoke sin la var. [guai REQ-20260626]
- **P22** Artefactos de proceso resueltos contra el repo-root `docs_process_root` (nunca bajo `backend/`). [guai REQ-20260625]
- **P23** Ids secuenciales asignados leyendo el registry (máx+1), nunca de memoria. [clinica-dental LL-011; colisiones 3 ciclos]
- **P24** Tablas de trade-off con columna "efecto sobre el objetivo declarado". [facturacion LL-20260730]
- **P25** Desambiguación de codebase objetivo pre-Fase-1 (componente en >1 codebase ⇒ confirmación de una línea). [sie-integraciones; mis-scope de 406K]

### story-refiner
- **P26** Gate reuse-before-create: grep del componente base antes de especificar una vista nueva variante; "no se puede adaptar porque…" obligatorio; architect no congela duplicados. [guai DD-ILG; ~160K tirados]
- **P27** Rider AC de checklist de known-bugs del proyecto ("la implementación pasa <checklist> al primer test run"). [sie-v2; bugs DD-1031 re-generados]
- **P28** Mapas const con centinela tipados `number`, nunca `as const` (regla TS a stack profiles + AC). [clinica-dental LL-013]

### architect-reviewer / schema-freeze
- **P29** HALT por credenciales de probe: mecanismo durable git-ignorado; sin sesión viva al freeze = GATE blocker, nunca "ASSUMED, defer to smoke". [clinica-dental LL-015; defecto F50 escapó al smoke]
- **P30** Modelo de auth/identidad = pregunta bloqueante antes de todo freeze que dependa de él (guest vs login; nunca elegir la rama pesada por defecto). [guai S14; re-freeze completo]
- **P31** Ampliar enum/estado ⇒ ABRIR el schema de respuesta y leer el TIPO declarado (`Literal`/Enum = ítem de cambio explícito `file:line`). [guai S6; 500 en producción tras 3 revisiones]
- **P32** Freeze de schemas con alias camelCase registra el patrón de retorno que fuerza (instancia vs `model_dump(by_alias=True)`). [guai S4→S5: A/B probado, clase eliminada]
- **P33** E7: enumerar los campos que LEEN los gates downstream de un estado copiado/construido y probar que el freeze los puebla (semántica SQL `NULL = NULL`). [facturacion; loop infinito de login]
- **P34** Zero-row DML = fallo silencioso: `UPDATE/DELETE` que actúa de gate asserta affected-rows; parejas writer/reader con la misma clave prueban expresión idéntica. [facturacion; sobrevivió 3 fases + un ciclo]
- **P35** Blast-radius de sinks process-global (TLS callbacks, handlers globales): enumerar herederos; si el fix QUITA una relajación heredada, el smoke ejercita los canales herederos. [facturacion, CRITICAL 2 ciclos]
- **P36** Disciplina de bump de dependencias: siempre vía package manager; versión mínima que cierra el CVE; superficies no verificables = UNVERIFIED-until-restore; resolución de deps como precondición P0 del smoke. [facturacion; 3 iteraciones de smoke ciegas]
- **P37** Patrón nuevo sin precedente in-repo cita ≥1 ejemplo o justificación + comando exacto de aplicación en deploy. [sie-v2]
- **P38** Auditoría de cobertura de verificación como segundo objeto de Fase 3 ("¿qué estados alcanzables no ejercita ningún caso?" + tabla estado→caso). [facturacion; 31 casos y ninguno el double-save que ocultó el BLOCKER]
- **P39** Versiones de runtime/deps leídas del artefacto instalado, nunca de docs (doc que contradice = stale). [dbcanvas Electron 41-vs-34; familia recurrió en sie]

### migration-reviewer
- **P40** Datos de dominio en migración: buscar mecanismo de seeds existente primero; si hay, `INSERT` en migración = BLOCKER de convención + alternativa con comando. [sie-v2 DD-896; ~120K rework]
- **P41** Drift migración ↔ `SCHEMA-FROZEN`: toda columna/FK/índice/enum de la migración debe estar en el schema congelado; drift = BLOCKER. [sie-v2]
- **P42** Tests de downgrade en DB compartida restauran baseline COMPLETO seeded en `finally` (drop schema + upgrade + reseed). [guai S14; 10 fallos no relacionados]

### code-reviewer
- **P43** Grep `.stringify(` en write paths de documentos estructurados; round-trip de serializador sobre doc persistido = BLOCKER. [kuraka-control LL-013]
- **P44** Walks de filesystem clasifican con `stat`/`lstat`, nunca bits del dirent (symlinks). [kuraka-control LL-012]
- **P45** Guards de campos opcionales en payloads (`value || undefined`, nunca `''` crudo); "espejar módulo X" reproduce sus guards, no solo la forma. [clinica-dental LL-014; 2 BLOCKERs]
- **P46** Detección de zero-submit: botón submit fuera de su `<form>` sin `form=` = no-op silencioso (complementa el check de doble-submit). [clinica-dental; 4 modales]
- **P47** Builder único normalizador de URLs de assets; concat manual base+path = flag. [clinica-dental LL-008; `/storage/storage/`]
- **P48** Clases de template existen en la hoja de estilos/tokens (grep antes de usar; reviewer verifica hunks de template). [guai; "botones invisibles" con gates verdes]
- **P49** Pairing de respuestas batch por clave de negocio estable, nunca por índice/orden. [sie-v2 DD-1397; 41+ registros cruzados en silencio]
- **P50** Prohibir fallbacks silenciosos de config (`or "..."`, `_DEFAULT`/`_FALLBACK`): falta config ⇒ error nombrando el campo. [sie-v2; realm Keycloak mal configurado invisible hasta prod]
- **P51** `git diff --cached` antes de commitear (un `git add` de archivo entero puede arrastrar una hand-edit del IDE). [guai]

### security-reviewer
- **P52** "Probar el seam congelado, no asertarlo": trazar invariantes a su ubicación, ejecutar vectores adversariales vivos contra store temporal, proof-by-compilation para claims de tipos. [kuraka-control; 5.5 pasó de 48 a 3-5 min con evidencia concluyente]
- **P53** Cambio de entropía de identificadores: id que pasa de aleatorio a secuencial ⇒ enumerar TODOS los endpoints que lo aceptan y confirmar ownership antes de actuar; superficies públicas usan token high-entropy aparte. [guai; IDOR silencioso]
- **P54** Endpoints de dinero: auth antes de la acción monetaria, idempotency key, retries acotados, ids de cliente ligados al owner, sin secretos de pago/PII en logs. [guai]
- **P55** Guard de alcance de Fase 5.5: revisa el diff de ESTE ciclo; nunca invoca flujos de scanner/pentest aparte. [guai; mis-invocación]
- **P56** Ledger vivo `DEFERRED-FINDINGS.md` de hallazgos diferidos/riesgo aceptado, re-confirmado por ciclo de seguridad. [facturacion]
- **P57** Triage de clasificación de secretos: credencial verificable → hash; secreto recuperable → cifrado reversible, nunca hash; lifecycle primero (dead code = borrar); el fix no introduce un bootstrap secret nuevo. [facturacion LL-20260720]
- **P58** Triage de findings de scanner contra código real (enumerar callers antes de clasificar; nunca heredar el "N mecánicos" del scan). [facturacion]
- **P59** Regla bidireccional de plegado de 5.5 en `kuraka-modes`: colapsable en proyecciones read-only; dedicada OBLIGATORIA con paths de cliente, writes al store gobernado o gates de alta confianza. [kuraka-control, validado 4×]

### test-engineer / e2e / frontend
- **P60** TEST-PLAN pinea archivo + describe-block/patrón grep, nunca títulos exactos pre-implementación. [kuraka-control S5c; 4/8 driftearon]
- **P61** Singletons process-global (circuit breakers, caches) se resetean vía API pública en fixture autouse; síntoma: fallos orden-dependientes. [sie-v2; 7 tests envenenados]
- **P62** Identidad de error asertada sobre el campo programático (`.code`/enum), nunca `str(exc)`. [sie-v2]
- **P63** Route provenance en smokes/integración (asertar la URL golpeada; verde contra handler legacy = FAIL) + un rojo de Fase 6 que revela bug real es BLOCKER a reportar, jamás skip/xfail. [guai ×2]
- **P64** Flags de supresión de eventos se auto-limpian en el mismo turno (`setTimeout(…,0)`); reviewer los marca "runtime-smoke required (chained)". [clinica-dental LL-010]
- **P65** Smoke ENCADENADO para features de gestos/puntero: ≥3 acciones secuenciadas asertando la invariante tras CADA paso. [clinica-dental LL-010]
- **P66** Trigger de verificación browser para flujos FE: gates automáticos son ciegos a navegación/polling/visual; ciclo que cambia flujo FE ⇒ smoke browser happy-path (6.5 no omitible solo porque falte Playwright: se hace vivo o se flaggea riesgo) — y sin `test_cmd` FE declarado, el gate por-story exige manejar la UI viva asertando estado concreto. [guai 3/4 defectos invisibles + dbcanvas overlay]
- **P67** "Tests reales cuando el runner existe": cada story FE entrega specs del código que cambia; diferir a Fase 6 se declara explícito. [guai; 52 casos aterrizaron tarde]
- **P68** Downgrades de interacción (touch→click, teclado fuera, debounce quitado) = desviación reportable con línea `⚠ downgrade:`. [clinica-dental]
- **P69** Curl-first para 403/404 de assets estáticos (sin auth header primero; 200 = bug de URL, no de auth). [clinica-dental LL-007]
- **P70** Check determinista de "artefactos nuevos invisibles": refs runtime introducidas (icon ids, translation keys, lazy routes) menos conjunto definido = vacío; ruta lazy nueva ⇒ reinicio del dev server antes del smoke. [clinica-dental LL-012]

### deployment-verifier
- **P71** Re-derivar todo claim numérico del run ACTUAL (ya institucionalizado en code-reviewer; ausente aquí). [guai; 536 vs 616]
- **P72** Checks de cadena de migraciones en deploy: baseline desplegado confirmado, upgrade aplica desde esa revisión, DDL idempotente, migraciones corren en UN solo lugar, seeds aislados e idempotentes. [guai]

### orquestador / skills
- **P73** Smoke de superficies de escritura contra copia temporal desechable + md5 del store real intacto. [kuraka-control, validado 3 ciclos]
- **P74** T9: post-Fase-4, ejercitar el write path nuevo (happy + idempotencia) contra store temporal antes de Fase 5. [kuraka-control; cazó no-op→404]
- **P75** Branch-currency pre-flight: `git log HEAD..<integration> --oneline | wc -l`; materialmente atrás ⇒ waiver explícito. [sie-v2; ciclo 64 commits atrás]
- **P76** Codificar el atajo "surgical story" con sus 4 condiciones (causa raíz única confirmada, contrato reproducido, sin schema/endpoint/migración, sin boundary compartido). [sie-v2 DD-1397]
- **P77** Durabilidad de artefactos por paso: `docs/process/**` y skills se commitean/backupean en el MISMO paso en que se escriben. [sie-v2; artefactos destruidos 3× por branch switch]
- **P78** Disciplina de costo/alcance/honestidad del orquestador: no presentar expansiones propias como menú pre-aprobado; estimaciones ancladas al scope generado; claims de mecanismo = hipótesis hasta reproducirse. [sie-v2; ~700K de scope no pedido, 1.85M vs 700-900K estimado]
- **P79** Defectos diferidos ⇒ archivo `FOLLOWUP-*.md` propio, nunca solo prosa del RETRO. [sie-v2 ×2]
- **P80** Implementador se detiene y reporta ante auto-contradicción del freeze/story (header vs cuerpo), citando ambas `file:line`. [facturacion; salvó dos ciclos sin estar codificado]
- **P81** GATE-FILTER 3Q antes de poner un ítem ante el usuario (¿lo introduce este ciclo? ¿está en scope? ¿bloquea el cierre?); lo demás va a "Información — sin decisión". [facturacion; el usuario fue el filtro 2 veces]
- **P82** Verificación de cierre a 0 tokens: `git merge-base --is-ancestor` por commit del checkpoint (un reset puede dejar commits colgando), `git show --stat` (contaminación), leer veredicto del smoke, checkpoint actualizado en el mismo acto que cierra la fase; T9 aplica a los artefactos del propio orquestador. [facturacion; 2 commits "entregados" eran dangling objects]
- **P83** Reclasificación de presupuesto pre-freeze: over-budget de Fases 2/2.5/3 se anota "costo esperado de verificación pre-freeze" cuando los tool_uses compraron hallazgos reales; warnings duros solo post-freeze. [sie-v2 + `aggregate-telemetry.py`]

---

## TIER 3 — Condicional / backlog

- **P84** Caps de review: soft-warn + hard-checkpoint (estado parcial persistido), allowance mayor para módulos espejo ≥15 archivos. [clinica-dental — su propio retro dice esperar confirmación de recurrencia por pattern-detector]

---

## Ya aplicado en esta sesión (pre-aprobación de este doc, reversible)

- Agente nuevo `jira-ticket-sync` generalizado (origen sie-integraciones) + routing/registro.
- Fix AppleDouble en `kuraka_common.py` + purga del snapshot basura de facturacion-honorarios.
- Versionado de suite (SUITE-VERSION 1.1.0, changelog, estampado en manifest y meta de ciclos).
- Comando `/kuraka-harvest` (con el escaneo de ambos canales).
