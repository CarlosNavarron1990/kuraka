# EVIDENCE — incident registry behind the framework rules

Every hard rule in the Kuraka prompts was earned by a real incident. Prompts
cite incidents by ID only (`(REQ-20260703)`, `(LL-017)`); the narrative lives
here so the rules stay slim without losing their provenance. When compacting a
prompt, move the story HERE and leave the ID.

| ID | What happened | Cost | Rules it spawned |
|---|---|---|---|
| DD-896 (FM-02) | 3 distinct S1 bugs surfaced only in Phase 6 after implementing 7 stories in batch | Debugging storm | T6 sequential implementation + per-story `make test` |
| DD-1031 | Fragmented scope + reversible decisions without a recorded value test; parallel batch accumulated S1+S3+S4 bugs; one near-revert; duplicate migration work; uncommitted cycle nearly lost | ≈13 runtime iterations | GATE0 consistency check; Phase-4 commit-per-story rule; T6 |
| REQ-20260611 S3 | Gate ran `make ... \| tail` — shell reported the pipe's exit code while the suite failed at collection; story advanced on a FALSE GREEN | Undetected red gate | T7 gate integrity → now the `gate_integrity` hook |
| REQ-20260625 (clinica-dental) | A 2-line fix cost 154K tokens: the agent re-read module + freeze + full review | 154K tokens | T8 pre-extracted digest for fix-runs |
| clinica-dental (varios) | Double-POST from two wired submit paths (recurred twice); a 7-vs-14-field contract gap caught only by a one-off instruction; "hidden = no access" masked a route-slash permission bug for a full extra cycle | 1+ extra cycles | Reviewer directed checks (single-submit, contract cross-check, normalize-before-compare); e2e rule 9 (empty vs broken) |
| REQ-20260703 (guai welcome-email) | Story explicitly excluded a second `register` endpoint; the developer wired it anyway and claimed "untouched, verified by git diff"; code-reviewer AND security-reviewer quoted the claim instead of running the diff (325K tokens, both missed it); two 0-token orchestrator gates caught it. Same cycle: a story-refiner re-run re-pasted the full package (171K tokens / 2 tool uses); smoke "passed" against legacy `/api/auth/register` masking the drift; an order-dependent guard test went false-green | ~245K + revert loop | T9 reproduce-never-quote; scope-fidelity checks in dev+reviewers; T10 delta re-runs; e2e rule 10 route provenance; isolated guard tests (T7) |
| REQ-20260801 | Telemetry recorded 19 of 31 runs (reported 3.76M vs real 5.90M, +57%); an 8-edit delta via resume cost 416.9K (+13.7% vs the run it fixed); analysis phases ran 4-for-4 over budget at ~1.9× re-deriving what the REQ already cited, while Phase-4 batches with strict two-file read lists ran 12-for-12 in band; a healthy agent was declared DEAD at ~7h on stale FIRST-file mtimes and gated mid-flight; an AskUserQuestion option asserted catalog images lived in the backend image (false — runtime volume copy); only the backend suite was baselined, so a mid-cycle `npm run lint` failure had no baseline to judge against | 2.1M+ waste | Telemetry-immediately rule → now the `telemetry_append` hook; T10 fresh-agent deltas; T1.1 analysis digests; liveness protocol (files, not clock) → `liveness_watch.sh`; T9.1 orchestrator claims; T7.1 baseline every gate per workspace |
| REQ-20260804 (audit-columns) | story-refiner ran 111 tool uses vs cap 30 (3.7×) and test-engineer 100/50 with zero warnings (only tokens were checked) — yet that pass found a real REQ bug; resumed agents reported cumulative tokens inflating the cycle +37% (2.73M reported vs ~1.99M real); migration `000020` from a parallel branch landed mid-cycle and produced 3 chain failures diagnosed by hand | Diagnóstico manual + métricas falsas | tool_uses_ok measurement → now `maxTurns` backstop; `resumed`/`tokens_incremental` telemetry fields; baseline_migration_head re-check; membership (not equality) chain assertions |
| kuraka-control LL-014 | An invalid `as string` cast rode green ~3 cycles because the Phase-4 gate ran vitest only (per-file transpile, no graph typecheck) | 3 cycles latent | Definition of "green" = lint + typecheck + test |
| kuraka-control S5b/S5c/P1 | Both implementer runs were truncated by session limits exactly in their self-verify tail (harmless — the orchestrator owned verification); a single-writer grep matched 5 files (`rm` hit "confi**rm**", `-v test` ate "at**test**"); a missing smoke doc was flagged BLOCKER in Phase 5 though Phase 6.8 owned it; code-reviewer ran 25–58 min in 4/8 cycles re-reading files one by one | Latencia + re-adjudicación | READY-FOR-EXTERNAL-VERIFY; architect rule 10 (re-run every pinned grep, word boundaries); DEFERRED severity; T8 reviewer digest |
| guai (config churn) | "Every environment must add N required vars" introduced then reverted | ~408K tokens | po-analyst rule 11 config classification |
| guai (webhook contract) | A webhook was built TWICE against a fabricated contract (described, not captured) | ~48% of a 4.16M-token cycle | Contract-first GATE (observe, don't recall) |
| guai / facturacion-honorarios (patches) | 3 consecutive cycles with 9→5 proposed patches almost all un-landed; facturacion landed 0/11 with the framework tier at 0%, same fixes re-proposed 4 retros running | Improvement loop dead | Prior-Retro Application Check as closing GATE; OPEN FRAMEWORK DEBT counters (now in final-auditor agent memory) |
| facturacion-honorarios (USREMAIL) | Six evidence failures across three agents + orchestrator: REQ built on a false "no validator exists" premise; a finding sourced from a comment on a fully commented-out body; a freeze header rejecting a decision its body still implemented | 1 ciclo confundido | Rule 19 (R-CUERPO / R-CONTROL / R-ESPEJO) — now always-on via the managed CLAUDE.md import |
| adela · dbcanvas · sie-v2 | Cache invalidation cleared the parent key but not derived sub-keys (`KEY:last_sync`, `KEY:{id}`); unit tests structurally can't catch it (caches cleared between cases); stale-subkey bugs reached production in 3 projects | Bugs en producción | Reviewer directed check: cache-namespace invalidation completeness |
| guai horarios | "3 slots from 1 add" anomaly was a hidden sibling dialog missed by a subtree snapshot | Falsa anomalía | e2e rule 8 full-page snapshots |
| LL-009 / LL-016 / LL-017 / LL-020 | Response-shape drift as #1 source of false reds; shared `storageState` authenticating only the first browser context; diagnostic-suite discipline (annotations, expected-reds); locale-formatted readonly datepickers | Falsos rojos/verdes E2E | e2e rules 11–14 + CRUD playbook (contexts/e2e-tester-rules.md) |
| sie DD1243 | 8 pre-existing red suites made a raw full-suite gate meaningless | Gate inutilizable | `baseline_red` semantics ("green" = no regression vs baseline) |
